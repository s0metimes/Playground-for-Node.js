// commonjs
// import // import any exported.

// ex) import { ZipCodeValidator } from "./ZipCodeValidator";

// export // this can be used for Any Declaration.

// ex) export interface StringValidator {}
// ex) export * from "./StringValidator";

// ~~ as ~~ from ~~ 은 rename 해서 import 혹은 export 하고 싶을 때 사용.

// default export => import ~~ from "~~";

// 전체 export 들 내보내기.
// import * as nickName from "nicm_name";

// default 붙은거 내보내기.
// import nickName from "nick_name";

// default 를 nickname 으로 rename 해서 내보내기.
// import default as nickname from "nick_name";